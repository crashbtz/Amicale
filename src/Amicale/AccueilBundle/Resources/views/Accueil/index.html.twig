{% extends "AmicaleAccueilBundle::layout.html.twig" %}

{% block title %}
    {{ parent() }} - Accueil
{% endblock %}


{% block stylesheets %}
    {{ parent() }}
    {% stylesheets '@AmicaleAccueilBundle/Resources/public/css/frontend/*' %}
<link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
    {% endstylesheets %}
{% endblock %}

{% block amicaleaccueil_body %}
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span10">
            <div id="carousel">
                    <div id="myCarousel" class="carousel slide">
                        <div class="carousel-inner thumbnail">
                        {% for produit in produits %}
                            {% if loop.first %}
                            <div class="item active"> 
                                <img src="{{ asset(produit.photoPath) }}" alt="{{ produit.photoAlt }}" />
                                <div class="carousel-caption">
                                    <p>{{ produit.titre }}</p>
                                </div>
                            </div>
                            {% else %}
                            <div class="item"> 
                                <img src="{{ asset(produit.photoPath) }}" alt="{{ produit.photoAlt }}" />
                                <div class="carousel-caption">
                                    <p>{{ produit.titre }}</p>
                                </div>
                            </div>
                            {% endif %}
                        {% endfor %}
                        </div>
                          
                        <a class="carousel-control left" data-slide="prev" href="#myCarousel">‹</a>
                        <a class="carousel-control right" data-slide="next" href="#myCarousel">›</a>
                    </div>
                </div>
            <div id="presentation">
                <p>Bonjour à tous,</p>
                <p>Vous voici sur le site de l'Amicale du personnel de l'hopital Charles Perrens.</p>
                <p>Vous y trouverez tous nos nouveaux produits ainsi que les différentes expositions, offres de nos partenaires et actualités diverses.</p>
                <p>Vous avez également la possibilité, si vous êtes connecté, de passer vos commandes sur ce site et vous serez alerté dès sa réception.</p>
            </div>
        </div>           


        <div class="span2">
        </div>


        </div>
    </div>
{% endblock %}